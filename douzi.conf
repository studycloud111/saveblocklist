    upstream douzihk {
    	hash $remote_addr;
    	server 103.135.102.178:12349 max_fails=3 fail_timeout=30s;
    }
    upstream douzisg {
        hash $remote_addr;
        server 206.189.95.192:12349 max_fails=3 fail_timeout=30s;
    }
    upstream douzijp {
        hash $remote_addr;
        server 172.104.67.214:12349 max_fails=3 fail_timeout=30s;
    }
    upstream douziuk {
        hash $remote_addr;
        server 143.47.249.213:12349 max_fails=3 fail_timeout=30s;
    }
     upstream douzius {
        hash $remote_addr;
        server 23.95.214.36:12349 max_fails=3 fail_timeout=30s;
    }
     upstream douzide {
        hash $remote_addr;
        server 138.2.133.156:12349 max_fails=3 fail_timeout=30s;
    }
     upstream douzitw {
        hash $remote_addr;
        server hinet1.camdvr.org:12349 max_fails=3 fail_timeout=30s;
    }
     upstream douziin {
        hash $remote_addr;
        server 45.79.117.46:12349 max_fails=3 fail_timeout=30s;
    }
     upstream douzikr {
        hash $remote_addr;
        server 138.2.133.156:12349 max_fails=3 fail_timeout=30s;
    }
     upstream douzica {
        hash $remote_addr;
        server 132.145.109.63:12349 max_fails=3 fail_timeout=30s;
    }
    server {
        listen              443 ssl;                   
        server_name douzihk.ovogreat.com;
        ssl_protocols       TLSv1.2 TLSv1.3;      
        
        ssl_certificate /usr/local/soga/ovocloud.crt; 
        ssl_certificate_key /usr/local/soga/privateovo.key; 
        ssl_session_cache   shared:SSL:10m;                                                          
        ssl_session_timeout 10m;
        proxy_connect_timeout 5s;  # 与被代理服务器建立连接的超时时间为5s
    	proxy_timeout 20s;   # 获取被代理服务器的响应最大超时时间为20s
        proxy_next_upstream on;  # 当被代理的服务器返回错误或超时时，将未返回响应的客户端连接请求传递给upstream中的下一个服务器
        proxy_next_upstream_tries 3;   # 转发尝试请求最多3次
        proxy_next_upstream_timeout 10s;    # 总尝试超时时间为10s
        proxy_socket_keepalive on;  # 开启SO_KEEPALIVE选项进行心跳检测
        proxy_protocol    on;
        proxy_pass        douzihk;
    }
    server {
        listen              443 ssl;                   
        server_name douzisg.ovogreat.com;
        ssl_protocols       TLSv1.2 TLSv1.3;      

        ssl_certificate /usr/local/soga/ovocloud.crt; 
        ssl_certificate_key /usr/local/soga/privateovo.key; 
        ssl_session_cache   shared:SSL:10m;            
                                               
        ssl_session_timeout 10m;
        proxy_connect_timeout 5s;  # 与被代理服务器建立连接的超时时间为5s
    	proxy_timeout 20s;   # 获取被代理服务器的响应最大超时时间为20s
        proxy_next_upstream on;  # 当被代理的服务器返回错误或超时时，将未返回响应的客户端连接请求传递给upstream中的下一个服务器
        proxy_next_upstream_tries 3;   # 转发尝试请求最多3次
        proxy_next_upstream_timeout 10s;    # 总尝试超时时间为10s
        proxy_socket_keepalive on;  # 开启SO_KEEPALIVE选项进行心跳检测
        proxy_protocol    on;
        
        proxy_pass        douzisg;

    }
    server {
        listen              443 ssl;                   
        server_name douzijp.ovogreat.com;
        ssl_protocols       TLSv1.2 TLSv1.3;      

        ssl_certificate /usr/local/soga/ovocloud.crt; 
        ssl_certificate_key /usr/local/soga/privateovo.key; 
        ssl_session_cache   shared:SSL:10m;            
                                                     
        ssl_session_timeout 10m;
        proxy_connect_timeout 5s;  # 与被代理服务器建立连接的超时时间为5s
        proxy_timeout 20s;   # 获取被代理服务器的响应最大超时时间为20s
        proxy_next_upstream on;  # 当被代理的服务器返回错误或超时时，将未返回响应的客户端连接请求传递给upstream中的下一个服务器
        proxy_next_upstream_tries 3;   # 转发尝试请求最多3次
        proxy_next_upstream_timeout 10s;    # 总尝试超时时间为10s
        proxy_socket_keepalive on;  # 开启SO_KEEPALIVE选项进行心跳检测
        proxy_protocol    on;
        proxy_pass        douzijp;

    }
    server {
        listen              443 ssl;                   
        server_name douziuk.ovogreat.com;
        ssl_protocols       TLSv1.2 TLSv1.3;      

        ssl_certificate /usr/local/soga/ovocloud.crt; 
        ssl_certificate_key /usr/local/soga/privateovo.key; 
        ssl_session_cache   shared:SSL:10m;            
                                               
        ssl_session_timeout 10m;
        proxy_connect_timeout 5s;  # 与被代理服务器建立连接的超时时间为5s
        proxy_timeout 20s;   # 获取被代理服务器的响应最大超时时间为20s
        proxy_next_upstream on;  # 当被代理的服务器返回错误或超时时，将未返回响应的客户端连接请求传递给upstream中的下一个服务器
        proxy_next_upstream_tries 3;   # 转发尝试请求最多3次
        proxy_next_upstream_timeout 10s;    # 总尝试超时时间为10s
        proxy_socket_keepalive on;  # 开启SO_KEEPALIVE选项进行心跳检测
        proxy_protocol    on;
        proxy_pass        douziuk;
    }
    server {
        listen              443 ssl;                   
        server_name douzius.ovogreat.com;
        ssl_protocols       TLSv1.2 TLSv1.3;      
        
        ssl_certificate /usr/local/soga/ovocloud.crt; 
        ssl_certificate_key /usr/local/soga/privateovo.key; 
        ssl_session_cache   shared:SSL:10m;            
                                           
        ssl_session_timeout 10m;
        proxy_connect_timeout 5s;  # 与被代理服务器建立连接的超时时间为5s
        proxy_timeout 20s;   # 获取被代理服务器的响应最大超时时间为20s
        proxy_next_upstream on;  # 当被代理的服务器返回错误或超时时，将未返回响应的客户端连接请求传递给upstream中的下一个服务器
        proxy_next_upstream_tries 3;   # 转发尝试请求最多3次
        proxy_next_upstream_timeout 10s;    # 总尝试超时时间为10s
        proxy_socket_keepalive on;  # 开启SO_KEEPALIVE选项进行心跳检测
        proxy_protocol    on;
        proxy_pass        douzius;

    }
    server {
        listen              443 ssl;                   
        server_name douzide.ovogreat.com;
        ssl_protocols       TLSv1.2 TLSv1.3;      
        
        ssl_certificate /usr/local/soga/ovocloud.crt; 
        ssl_certificate_key /usr/local/soga/privateovo.key; 
        ssl_session_cache   shared:SSL:10m;            
                                          
        ssl_session_timeout 10m;
        proxy_connect_timeout 5s;  # 与被代理服务器建立连接的超时时间为5s
        proxy_timeout 20s;   # 获取被代理服务器的响应最大超时时间为20s

        proxy_next_upstream on;  # 当被代理的服务器返回错误或超时时，将未返回响应的客户端连接请求传递给upstream中的下一个服务器
        proxy_next_upstream_tries 3;   # 转发尝试请求最多3次
        proxy_next_upstream_timeout 10s;    # 总尝试超时时间为10s
        proxy_socket_keepalive on;  # 开启SO_KEEPALIVE选项进行心跳检测
        proxy_protocol    on;
        proxy_pass        douzide;

    }
    server {
        listen              443 ssl;                   
        server_name douzica.ovogreat.com;
        ssl_protocols       TLSv1.2 TLSv1.3;      
        
        ssl_certificate /usr/local/soga/ovocloud.crt; 
        ssl_certificate_key /usr/local/soga/privateovo.key; 
        ssl_session_cache   shared:SSL:10m;            
                                              
        ssl_session_timeout 10m;
        proxy_connect_timeout 5s;  # 与被代理服务器建立连接的超时时间为5s
        proxy_timeout 20s;   # 获取被代理服务器的响应最大超时时间为20s

        proxy_next_upstream on;  # 当被代理的服务器返回错误或超时时，将未返回响应的客户端连接请求传递给upstream中的下一个服务器
        proxy_next_upstream_tries 3;   # 转发尝试请求最多3次
        proxy_next_upstream_timeout 10s;    # 总尝试超时时间为10s
        proxy_socket_keepalive on;  # 开启SO_KEEPALIVE选项进行心跳检测
        proxy_protocol    on;
        proxy_pass        douzica;

    }
    server {
        listen              443 ssl;                   
        server_name douziin.ovogreat.com;
        ssl_protocols       TLSv1.2 TLSv1.3;      
        
        ssl_certificate /usr/local/soga/ovocloud.crt; 
        ssl_certificate_key /usr/local/soga/privateovo.key; 
        ssl_session_cache   shared:SSL:10m;            
                                              
        ssl_session_timeout 10m;
        proxy_connect_timeout 5s;  # 与被代理服务器建立连接的超时时间为5s
        proxy_timeout 20s;   # 获取被代理服务器的响应最大超时时间为20s

        proxy_next_upstream on;  # 当被代理的服务器返回错误或超时时，将未返回响应的客户端连接请求传递给upstream中的下一个服务器
        proxy_next_upstream_tries 3;   # 转发尝试请求最多3次
        proxy_next_upstream_timeout 10s;    # 总尝试超时时间为10s
        proxy_socket_keepalive on;  # 开启SO_KEEPALIVE选项进行心跳检测
        proxy_protocol    on;
        proxy_pass        douziin;

    }
    server {
        listen              443 ssl;                   
        server_name douzitw.ovogreat.com;
        ssl_protocols       TLSv1.2 TLSv1.3;      
        
        ssl_certificate /usr/local/soga/ovocloud.crt; 
        ssl_certificate_key /usr/local/soga/privateovo.key; 
        ssl_session_cache   shared:SSL:10m;            
                                           
        ssl_session_timeout 10m;
        proxy_connect_timeout 5s;  # 与被代理服务器建立连接的超时时间为5s
        proxy_timeout 20s;   # 获取被代理服务器的响应最大超时时间为20s

        proxy_next_upstream on;  # 当被代理的服务器返回错误或超时时，将未返回响应的客户端连接请求传递给upstream中的下一个服务器
        proxy_next_upstream_tries 3;   # 转发尝试请求最多3次
        proxy_next_upstream_timeout 10s;    # 总尝试超时时间为10s
        proxy_socket_keepalive on;  # 开启SO_KEEPALIVE选项进行心跳检测
        proxy_protocol    on;
        proxy_pass        douzitw;
    }
    server {
        listen              443 ssl;                   
        server_name douzikr.ovogreat.com;
        ssl_protocols       TLSv1.2 TLSv1.3;      
        
        ssl_certificate /usr/local/soga/ovocloud.crt; 
        ssl_certificate_key /usr/local/soga/privateovo.key; 
        ssl_session_cache   shared:SSL:10m;                                                 
        ssl_session_timeout 10m;
        proxy_connect_timeout 5s;  # 与被代理服务器建立连接的超时时间为5s
        proxy_timeout 20s;   # 获取被代理服务器的响应最大超时时间为20s

        proxy_next_upstream on;  # 当被代理的服务器返回错误或超时时，将未返回响应的客户端连接请求传递给upstream中的下一个服务器
        proxy_next_upstream_tries 3;   # 转发尝试请求最多3次
        proxy_next_upstream_timeout 10s;    # 总尝试超时时间为10s
        proxy_socket_keepalive on;  # 开启SO_KEEPALIVE选项进行心跳检测
        proxy_protocol    on;
        proxy_pass        douzikr;

    }
